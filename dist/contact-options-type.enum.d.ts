/**
 * Represents preferred contact channels.
 * Mirrors the Prisma enum `ContactOptionsType`.
 */
export declare enum ContactOptionsType {
    EMAIL = "EMAIL",
    PHONE = "PHONE",
    SMS = "SMS",
    WHATSAPP = "WHATSAPP",
    LETTER = "LETTER"
}
